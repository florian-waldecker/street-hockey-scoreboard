# Tooling for working on the scoreboard (not needed to run it).
#   python -m venv .venv-dev && .venv-dev\Scripts\pip install -r requirements-dev.txt
-r requirements.txt
pytest~=8.4
httpx~=0.28
ruff~=0.16
