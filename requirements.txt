# Pinned to compatible-release ranges so the one-click .bat rebuilds the same
# stack every time. Bump deliberately, not by accident.
fastapi~=0.141.1
uvicorn[standard]~=0.52.4
python-multipart~=0.0.32
# WebSocket transport. Comes in via uvicorn[standard] too; pinned explicitly
# because the whole app runs over /ws.
websockets~=16.1
